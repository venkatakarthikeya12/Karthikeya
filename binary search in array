#include<stdio.h>
int main() {
int n,m=0;
printf("enter a size of an array :");
scanf(" %d ",&n);
int a[n];
printf("enter a elements of array : ");
for(int i=0;i<n;i++) {
scanf(" %d ",&a[i]); }
int x;
printf("enter a number ");
scanf("%d",&x); 
int max,min,mid;
max = n-1;
min = 0;
while(min <= max) {
if(min + max / 2 ) {
